# UI Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Довести UI до production-grade: убрать невалидные Tailwind-классы, починить интерактивность карточек, унифицировать токены типографики, добавить reduced-motion для scroll-behavior, добавить will-change на persistent-анимации.

**Architecture:** Чистые точечные изменения в существующих компонентах. Никаких новых компонентов, никаких изменений смысла/структуры/контента. Все изменения обратно совместимы.

**Tech Stack:** Next.js 14 App Router, TypeScript strict, Tailwind CSS, Framer Motion 12

---

## File Map

| Файл | Изменение |
|------|-----------|
| `app/globals.css` | Guard `scroll-behavior` with `prefers-reduced-motion` |
| `components/ui/Button.tsx` | Fix `hover:opacity-92` → `hover:opacity-90` |
| `app/not-found.tsx` | Nav offset, metadata, typography tokens |
| `components/layout/NavigationBar.tsx` | `type="button"`, `bg-base/95`, typography tokens, focus-visible |
| `components/layout/Footer.tsx` | Typography tokens, logo aria-label, contact link semantics |
| `components/sections/home/CasesPreviewSection.tsx` | Card links, remove raw `imageSrc` text, fix `group-hover` |
| `components/sections/services/PricingTable.tsx` | Hover states, typography tokens, aria-labelledby, emoji a11y |
| `components/sections/home/ProblemSolutionSection.tsx` | `will-change` on spinning rings |

---

### Task 1: CSS baseline + Button.tsx

**Files:**
- Modify: `app/globals.css`
- Modify: `components/ui/Button.tsx`

- [ ] **Step 1: Guard scroll-behavior with reduced-motion**

В `app/globals.css` замени:
```css
  html {
    scroll-behavior: smooth;
    text-size-adjust: 100%;
  }
```
на:
```css
  html {
    text-size-adjust: 100%;
  }

  @media (prefers-reduced-motion: no-preference) {
    html { scroll-behavior: smooth; }
  }
```

- [ ] **Step 2: Fix invalid opacity class in Button.tsx**

В `components/ui/Button.tsx` строка 41, замени:
```ts
    'hover:opacity-92 hover:shadow-glow-cta hover:-translate-y-px',
```
на:
```ts
    'hover:opacity-90 hover:shadow-glow-cta hover:-translate-y-px',
```

- [ ] **Step 3: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 4: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add app/globals.css components/ui/Button.tsx && git commit -m "fix(polish): guard scroll-behavior with reduced-motion, fix opacity-92 → opacity-90"
```

---

### Task 2: not-found.tsx — nav offset + metadata + typography

**Files:**
- Modify: `app/not-found.tsx`

- [ ] **Step 1: Заменить весь файл**

```tsx
import type { Metadata } from 'next'
import { Button } from '@/components/ui/Button'

export const metadata: Metadata = {
  title: '404 — Страница не найдена | AWSM',
  robots: { index: false, follow: false },
}

export default function NotFound() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-base dot-grid pt-16 md:pt-20">
      <div className="container text-center">
        <p className="font-mono font-bold text-[96px] leading-none text-primary/20 mb-4" aria-hidden="true">
          404
        </p>
        <h1 className="font-display font-bold text-heading-l md:text-display-m text-text-primary mb-4">
          Страница не найдена
        </h1>
        <p className="font-body text-body-l text-text-secondary mb-8 max-w-sm mx-auto">
          Возможно, страница была удалена или вы перешли по неверной ссылке.
        </p>
        <Button variant="primary" size="lg" href="/">
          ← Вернуться на главную
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 3: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add app/not-found.tsx && git commit -m "fix(polish): 404 — nav offset, metadata, typography tokens"
```

---

### Task 3: NavigationBar — type, bg-base/95, typography, focus-visible

**Files:**
- Modify: `components/layout/NavigationBar.tsx`

- [ ] **Step 1: Добавить `type="button"` на hamburger**

Найди строку 101:
```tsx
          <motion.button
            className="md:hidden text-text-secondary hover:text-text-primary p-2 transition-colors"
```
Замени на:
```tsx
          <motion.button
            type="button"
            className="md:hidden text-text-secondary hover:text-text-primary p-2 rounded-md transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40"
```

- [ ] **Step 2: Исправить невалидный `bg-base/97`**

Найди строку 127:
```tsx
            className="md:hidden bg-base/97 backdrop-blur-xl border-t border-border overflow-hidden"
```
Замени на:
```tsx
            className="md:hidden bg-base/95 backdrop-blur-xl border-t border-border overflow-hidden"
```

- [ ] **Step 3: Токены типографики + focus-visible на desktop ссылках**

Найди строку 70–75 (логотип):
```tsx
          <span className="font-display font-bold text-xl text-text-primary tracking-tight">
            AW<span className="text-cta">S</span>M
          </span>
          <span className="hidden sm:block text-text-muted text-xs font-body mt-0.5">
            Геомаркетинг. Результат.
          </span>
```
Замени на:
```tsx
          <span className="font-display font-bold text-heading-s text-text-primary tracking-tight">
            AW<span className="text-cta">S</span>M
          </span>
          <span className="hidden sm:block text-text-muted text-caption font-body mt-0.5">
            Геомаркетинг. Результат.
          </span>
```

Найди строку 84 (desktop nav links):
```tsx
                className="text-text-secondary hover:text-text-primary text-sm font-body font-medium transition-colors duration-200 relative group"
```
Замени на:
```tsx
                className="text-text-secondary hover:text-text-primary text-body-s font-body font-medium transition-colors duration-200 relative group rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40 focus-visible:ring-offset-2 focus-visible:ring-offset-base"
```

Найди строку 137 (mobile nav links):
```tsx
                    className="block text-text-primary text-lg font-body font-medium py-3 border-b border-border/40 last:border-0 hover:text-primary transition-colors"
```
Замени на:
```tsx
                    className="block text-text-primary text-body-l font-body font-medium py-3 border-b border-border/40 last:border-0 hover:text-primary transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40 focus-visible:ring-inset rounded-sm"
```

- [ ] **Step 4: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 5: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add components/layout/NavigationBar.tsx && git commit -m "fix(polish): nav — type=button, bg-base/95, typography tokens, focus-visible"
```

---

### Task 4: Footer — токены типографики + семантика контактов

**Files:**
- Modify: `components/layout/Footer.tsx`

- [ ] **Step 1: Заменить весь файл**

```tsx
import Link from 'next/link'
import { PLACEHOLDERS } from '@/constants/placeholders'
import { NAV_LINKS } from '@/constants/navigation'

const LEGAL_LINKS = [
  { label: 'Политика конфиденциальности', href: '/privacy' },
  { label: 'Договор-оферта', href: '/offer' },
]

export function Footer() {
  return (
    <footer className="bg-surface border-t border-border">
      <div className="container py-12 md:py-16">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-10 mb-10">
          {/* Brand */}
          <div>
            <Link href="/" aria-label="AWSM — на главную" className="inline-block mb-3">
              <span className="font-display font-bold text-heading-s text-text-primary">
                AW<span className="text-cta">S</span>M
              </span>
            </Link>
            <p className="text-text-muted text-body-s font-body leading-relaxed max-w-xs">
              Агентство гео-продвижения локального бизнеса.
              Выводим карточки в ТОП Яндекс.Карт, Google Maps и 2GIS.
            </p>
          </div>

          {/* Navigation */}
          <div>
            <p className="text-text-muted text-label font-mono uppercase tracking-widest mb-4">
              Навигация
            </p>
            <ul className="flex flex-col gap-3">
              {NAV_LINKS.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-text-secondary hover:text-text-primary text-body-s font-body transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary/40 rounded-sm"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
              <li>
                <Link
                  href="/audit"
                  className="text-cta hover:text-cta-hover text-body-s font-body font-medium transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary/40 rounded-sm"
                >
                  Получить аудит →
                </Link>
              </li>
            </ul>
          </div>

          {/* Contacts */}
          <div>
            <p className="text-text-muted text-label font-mono uppercase tracking-widest mb-4">
              Контакты
            </p>
            <ul className="flex flex-col gap-3 text-body-s font-body text-text-secondary">
              <li className="opacity-50 select-none" title="Контакт добавляется позже">
                {PLACEHOLDERS.phone}
              </li>
              <li className="opacity-50 select-none" title="Контакт добавляется позже">
                {PLACEHOLDERS.telegram}
              </li>
              <li className="opacity-50 select-none" title="Контакт добавляется позже">
                {PLACEHOLDERS.email}
              </li>
              <li className="text-text-muted text-caption pt-2">Пн–Пт 9:00–18:00 МСК</li>
            </ul>
          </div>
        </div>

        {/* Bottom */}
        <div className="pt-8 border-t border-border flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
          <p className="text-text-muted text-caption font-body">
            © 2026 AWSM. Все права защищены. {PLACEHOLDERS.requisites}
          </p>
          <div className="flex gap-6">
            {LEGAL_LINKS.map((link) => (
              <Link
                key={link.href}
                href={link.href}
                className="text-text-muted hover:text-text-secondary text-caption font-body transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-primary/40 rounded-sm"
              >
                {link.label}
              </Link>
            ))}
          </div>
        </div>
      </div>
    </footer>
  )
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 3: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add components/layout/Footer.tsx && git commit -m "fix(polish): footer — typography tokens, aria-label, placeholder opacity, focus-visible"
```

---

### Task 5: CasesPreviewSection — интерактивность карточек

**Files:**
- Modify: `components/sections/home/CasesPreviewSection.tsx`

- [ ] **Step 1: Обернуть карточку в Link, убрать imageSrc из плашки, починить group-hover**

Найди функцию `CaseCard` (строки 105–181). Замени её полностью:

```tsx
function CaseCard({ item }: { item: Case }) {
  const badgeVariant = CATEGORY_BADGE_VARIANT[item.category] ?? 'muted'
  const mainMetric   = item.metrics[0]
  const secondMetric = item.metrics[1]

  return (
    <Link href="/cases" className="group block h-full focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40 rounded-xl">
      <motion.article
        className="glass-card overflow-hidden flex flex-col h-full cursor-pointer"
        whileHover={{ y: -6 }}
        transition={{ duration: 0.22, ease: 'easeOut' }}
      >
        {/* Image placeholder — 16:9 */}
        <div
          className="relative w-full overflow-hidden"
          style={{ paddingBottom: '56.25%' }}
          aria-hidden="true"
        >
          <div
            className="absolute inset-0 flex items-center justify-center bg-surface-elevated"
            style={{ borderBottom: '1px dashed rgba(79,110,247,0.25)' }}
          >
            <div className="absolute top-3 left-3">
              <Badge variant={badgeVariant} size="sm">
                {CATEGORY_LABELS[item.category]}
              </Badge>
            </div>
            <ImageIcon size={32} color="rgba(79,110,247,0.25)" weight="thin" />
          </div>
        </div>

        {/* Content */}
        <div className="p-6 flex flex-col flex-1 gap-4">
          <div>
            <h3 className="font-display font-bold text-heading-s text-text-primary mb-1">
              {item.title}
            </h3>
            <p className="font-body text-body-s text-text-muted">{item.city}</p>
          </div>

          <div className="grid grid-cols-2 gap-3 py-4 border-y border-border">
            <MetricCell value={mainMetric.value} label={mainMetric.label} primary />
            {secondMetric && (
              <MetricCell value={secondMetric.value} label={secondMetric.label} />
            )}
          </div>

          <p className="font-body text-body-s text-text-secondary leading-relaxed flex-1">
            {item.description}
          </p>

          <div className="mt-auto pt-1">
            <span className="inline-flex items-center gap-1.5 font-body text-body-s text-primary font-medium group-hover:gap-2.5 transition-all duration-200">
              Читать кейс
              <ArrowRight size={14} weight="bold" aria-hidden="true" />
            </span>
          </div>
        </div>
      </motion.article>
    </Link>
  )
}
```

Также замени функцию `MetricCell`:

```tsx
function MetricCell({
  value,
  label,
  primary = false,
}: {
  value:   string
  label:   string
  primary?: boolean
}) {
  return (
    <div>
      <p className={`font-mono font-bold text-metric-sm leading-none mb-1 ${primary ? 'text-secondary' : 'text-primary'}`}>
        {value}
      </p>
      <p className="font-body text-caption text-text-muted leading-snug">{label}</p>
    </div>
  )
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 3: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add components/sections/home/CasesPreviewSection.tsx && git commit -m "fix(polish): case cards — Link wrapper, remove imageSrc text, fix group-hover, metric tokens"
```

---

### Task 6: PricingTable — hover states, токены, a11y

**Files:**
- Modify: `components/sections/services/PricingTable.tsx`

- [ ] **Step 1: Заменить весь файл**

```tsx
import { PRICING_PLANS, COMBO_OFFER } from '@/constants/pricing'
import { Button } from '@/components/ui/Button'
import { Badge } from '@/components/ui/Badge'
import { cn } from '@/lib/utils'

export function PricingTable() {
  return (
    <section className="section-padding bg-base" aria-labelledby="pricing-table-heading">
      <div className="container">
        <h2
          id="pricing-table-heading"
          className="font-display font-bold text-heading-l md:text-display-m text-text-primary mb-10 text-center"
        >
          Тарифы
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
          {PRICING_PLANS.map((plan) => (
            <div
              key={plan.id}
              className={cn(
                'glass-card p-7 flex flex-col transition-all duration-300 hover:-translate-y-1',
                plan.featured && 'border-primary/40 animate-pulse-border',
              )}
            >
              <div className="flex justify-between items-start mb-4">
                <h3 className="font-display font-bold text-heading-s text-text-primary">
                  {plan.name}
                </h3>
                {plan.featured && <Badge variant="cta">ХИТ</Badge>}
              </div>
              <p className="text-text-muted text-body-s font-body mb-6">{plan.tagline}</p>
              <ul className="flex flex-col gap-2.5 mb-8 flex-1">
                {plan.features.map((f) => (
                  <li key={f} className="flex gap-2 text-text-secondary text-body-s font-body">
                    <span className="text-primary flex-shrink-0" aria-hidden="true">•</span>
                    {f}
                  </li>
                ))}
              </ul>
              <p className="font-display font-bold text-metric-sm text-text-primary mb-4">
                {plan.price.toLocaleString('ru-RU')}{' '}
                <span className="text-text-secondary text-body-m font-body font-normal">
                  {plan.priceUnit}
                </span>
              </p>
              <Button
                variant={plan.featured ? 'primary' : 'secondary'}
                size="md"
                className="w-full"
                href="/audit"
              >
                {plan.ctaLabel}
              </Button>
            </div>
          ))}
        </div>

        <div className="glass-card p-6 text-center border-secondary/20">
          <p className="text-secondary font-mono text-body-s mb-1">
            <span role="img" aria-label="Подарок">🎁</span>{' '}
            {COMBO_OFFER.description}
          </p>
          <p className="text-text-primary font-body font-semibold text-body-m">
            {COMBO_OFFER.price.toLocaleString('ru-RU')}{' '}
            {COMBO_OFFER.priceUnit} · {COMBO_OFFER.saving}
          </p>
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 3: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add components/sections/services/PricingTable.tsx && git commit -m "fix(polish): pricing table — hover states, typography tokens, aria-labelledby, emoji a11y"
```

---

### Task 7: Animation performance — will-change на spinning rings

**Files:**
- Modify: `components/sections/home/ProblemSolutionSection.tsx`

- [ ] **Step 1: Добавить `will-change` на вращающиеся кольца**

В `ProblemSolutionSection.tsx` найди **outer ring** (≈ строка 37):
```tsx
      <motion.div
        className="absolute w-40 h-40 md:w-56 md:h-56 rounded-full border border-border-strong"
        animate={inView
```
Добавь `style={{ willChange: 'transform' }}`:
```tsx
      <motion.div
        className="absolute w-40 h-40 md:w-56 md:h-56 rounded-full border border-border-strong"
        style={{ willChange: 'transform' }}
        animate={inView
```

Найди **middle ring** (≈ строка 50):
```tsx
      <motion.div
        className="absolute w-28 h-28 md:w-40 md:h-40 rounded-full border border-primary/20"
        animate={inView
```
Добавь `style={{ willChange: 'transform' }}`:
```tsx
      <motion.div
        className="absolute w-28 h-28 md:w-40 md:h-40 rounded-full border border-primary/20"
        style={{ willChange: 'transform' }}
        animate={inView
```

- [ ] **Step 2: TypeScript check**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && npx tsc --noEmit
```

Ожидается: 0 ошибок.

- [ ] **Step 3: Commit**

```bash
cd "F:/Code/Agents/AWSM new/awsm-site" && git add components/sections/home/ProblemSolutionSection.tsx && git commit -m "perf(polish): will-change on persistent spinning ring animations"
```
